const sysGetUseCasesDtoInType = shape({
  useCase: string(),
  extended: boolean(),
  dtoType: string()
});

const sysGenerateDiagnosticReportDtoInType = shape({
  triggerGc: boolean(),
  includeHeapDump: boolean()
});
