// TODO Next major - remove this whole file (it's used only in projects using devkit <= 5.x).
let path = "./.eslintrc-uu5.js";
try {
  let pkg = require("./package.json", { paths: ["."] });
  let spec = pkg.spec;
  if (spec && spec.startsWith("nodejs-")) {
    path = "./.eslintrc-nodejs.js";
  }
} catch (e) {
  // use default path
}

module.exports = require(path);
