<script>
  (function () {
    if (window.Uu5Loader) {
      // POST_PROCESS_REMOVE_LINE <%= context.allowInitUuApp && "\n      var directDependencyMap = " + JSON.stringify(context.directDependencyVersionMap, null, 2).replace(/\n/g, "\n      ") || "" %>;
      // POST_PROCESS_REMOVE_LINE <%= "\n      " + context.loaderConfigSnippet.replace(/\n/g, "\n      ") %>
      // POST_PROCESS_REMOVE_LINE <%= "\n      " + context.loaderTopLevelExplicitConfigSnippet.replace(/\n/g, "\n      ") %>

      // POST_PROCESS_REMOVE_LINE <%= context.allowInitUuApp ? `
      Uu5Loader.initUuApp(directDependencyMap, bundledConfig, bundledConfigExplicit);
      // POST_PROCESS_REMOVE_LINE ` : /* case when initUuApp is disabled by build */ `
      Uu5Loader.config(bundledConfig);
      Uu5Loader.config(bundledConfigExplicit);
      // POST_PROCESS_REMOVE_LINE ` %>
    }
  })();
</script>