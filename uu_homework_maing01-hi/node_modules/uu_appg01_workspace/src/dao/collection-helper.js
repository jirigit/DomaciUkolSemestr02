"use strict";

/**
 * Auxiliary class for working with MongoDB collections.
 */
class CollectionHelper {

  static isSystemCollection(collectionName) {
    let result = collectionName.startsWith("system.");
    return result;
  }

}

module.exports = CollectionHelper;